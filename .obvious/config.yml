# .obvious/config.yml — Autobuild repo policy for pauljones454/TopNote
# Generated by autobuild-setup-worker on 2026-06-25

branches:
  defaultBase: main

pr:
  merge:
    method: squash

# TODO: Set preview URL when staging deployment is configured
# preview:
#   primaryEnvironment: staging
#   environments:
#     staging:
#       urlTemplate: https://pr-{prNumber}-staging.example.com
#       backendBaseUrl: https://api.staging.example.com
#   deploy:
#     pollIntervalSeconds: 30
#     timeoutMinutes: 10

# TODO: Add Slack channel ID for merge announcements
# notifications:
#   slack:
#     mergeAnnouncements:
#       channelId: C0000000000
#       channelName: topnote-deploys

